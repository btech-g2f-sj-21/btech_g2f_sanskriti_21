#include <stdio.h>
//to find sum and percentage
int main() {
  float a,b,c,sum,percentage;
    printf("Enter a,b and c:");
    scanf("%f %f %f",&a,&b,&c);
    sum=(a+b+c);
    percentage=(((a+b+c)/3)*100);
    printf("sum of the numbers is:%f",sum);
    printf("\n percentage of the numbers is:%f",percentage);
    return 0;
}
